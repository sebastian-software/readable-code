{
  "name": "s15e-javascript",
  "version": "0.2.4",
  "description": "JavaScript Coding Standard of Sebastian Software GmbH",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "release-it --github.release --npm.publish --non-interactive",
    "release:minor": "release-it --github.release --npm.publish --non-interactive --increment minor",
    "release:major": "release-it --github.release --npm.publish --non-interactive --increment major",
    "check": "eslint-find-rules -u .eslintrc.yml || true"
  },
  "keywords": [
    "lint",
    "es6",
    "eslint",
    "eslintconfig",
    "standard"
  ],
  "author": "Sebastian Werner <s.werner@sebastian-software.de>",
  "license": "Apache-2.0",
  "repository": "https://github.com/sebastian-software/s15e-javascript",
  "dependencies": {
    "eslint": "^3.8.1",
    "eslint-find-rules": "^1.14.0"
  }
}
