rules:
  declaration-bang-space-after: never
  declaration-bang-space-before: always
  declaration-block-no-duplicate-properties:
    - true
    - ignore: [ "consecutive-duplicates-with-different-values" ]

  declaration-block-no-redundant-longhand-properties: null
  declaration-block-no-shorthand-property-overrides: true
  declaration-block-semicolon-newline-after: always-multi-line
  declaration-block-semicolon-space-after: always-single-line
  declaration-block-semicolon-space-before: never
  declaration-block-single-line-max-declarations: 1
  declaration-block-trailing-semicolon: always
  declaration-colon-newline-after: always-multi-line
  declaration-colon-space-after: always-single-line
  declaration-colon-space-before: never
  declaration-empty-line-before: null
  declaration-no-important: true
  declaration-property-unit-blacklist:
    "/^animation/": ["s"]
    "/^transition/": ["s"]
    "line-height": ["px", "em", "rem"]

  declaration-block-properties-order:
    # Inspired by https://css-tricks.com/poll-results-how-do-you-order-your-css-properties/
    -
      # Preprocessor
      - properties: [ "@include", "@extend", composes ]
        order: "flexible"

      # Content
      - properties: [
          content, src
        ]

      # Positioning
      - properties: [
          position, z-index,
          overflow, box-sizing,
          display, float,
          top, right, bottom, left

      # Layout
      - properties: [
          flex, flex-basis, flex-direction, flex-flow, flex-grow, flex-shrink, flex-wrap,
          justify-content, align-items, align-content, align-self, order,
          min-width, width, max-width,
          min-height, height, max-height,
          padding, padding-top, padding-right, padding-bottom, padding-left,
          margin, margin-top, margin-right, margin-bottom, margin-left
        ]
        order: "flexible"

      # Appearance, Colors & Animation
      - properties: [
          background, box-shadow, border, outline, color, text-shadow, cursor,
          font, font-family, font-size, font-weight, font-style, line-height, text-align, list-style,
          opacity, animation, transform, transition, will-change, filter
        ]
        order: "flexible"

    - unspecified: bottomAlphabetical
